let arr = [1, 2, 3, 4, 5, 6, 7, 8, 9];
let obj = {
  role: {
    type: 123,
    allowNull: false,
    defaultValue: "1",
    primaryKey: false,
    autoIncrement: false,
    comment: null,
    field: "role",
  },
  status: {
    type: 123,
    allowNull: false,
    defaultValue: "0",
    primaryKey: false,
    autoIncrement: false,
    comment: null,
    field: "status",
  },
};
// let add = (item) => {
//   return item + 1;
// };
// console.log(arr.map((item) => add(item)));

// let print = (item) => {
//   console.log(item);
// };
// obj.map(print);
